<UserControl x:Class="Perceptron.OCR.UI.UserControls.RectanglePixel"
             x:Name="RectanglePixelControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:command="http://www.galasoft.ch/mvvmlight"
             xmlns:converters="clr-namespace:Perceptron.OCR.UI.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <UserControl.Resources>
        <converters:BooleanToFillConverter x:Key="BooleanToFillConverter" />
    </UserControl.Resources>

    <Rectangle x:Name="rectangle" Stroke="Black" StrokeThickness="0.25" Fill="{Binding ElementName=RectanglePixelControl, Path=Pixel.IsSelected, Converter={StaticResource BooleanToFillConverter}}">
        <i:Interaction.Triggers>
            <i:EventTrigger EventName="MouseLeftButtonDown">
                <command:EventToCommand Command="{Binding Path=SwitchSelectionCommand}" CommandParameter="{Binding ElementName=RectanglePixelControl, Path=Pixel}" />
            </i:EventTrigger>
        </i:Interaction.Triggers>
    </Rectangle>
    
</UserControl>
